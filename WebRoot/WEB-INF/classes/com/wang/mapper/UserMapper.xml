<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.itcast.core.dao.BaseDictDao" >
  
  <select id="selectCurrentPlans" resultType="com.wang.pojo.Plan" parameterType="Integer" >
    select *
      from plan
	 where user_id = #{id}
	 and NOW() &lt; end_time
  </select>
  
  <select id="selectAllPlans" resultType="com.wang.pojo.Plan" parameterType="Integer" >
    select *
      from plan
     where user_id = #{id}
  </select>
  <insert id="addPlan" parameterType="com.wang.pojo.Plan" >
    insert into plan 
    values (#{id},#{user_id},#{name},#{content},#{begin_time},
    #{end_time},#{expire},#{finish},#{punish_method},
    #{punish_content}
    )
  </insert>
   <select id="selectAllPlans" resultType="com.wang.pojo.Plan" parameterType="Integer" >
    select *
      from plan
     where user_id = #{id}
  </select>
</mapper>